"""Topology Explorer — NL2SQL 图谱可视化界面后端。

模块：

- :mod:`context_manager.api.retrieval`  ：NL → metric / dim / op 解析；子图扩展；SQL 草稿
- :mod:`context_manager.api.server`     ：FastAPI 路由（含 GET / POST /api/* + /static）

启动：

    python scripts/serve.py
    # 然后浏览器打开 http://localhost:8765
"""


__all__: list[str] = []
