# datapaw-skills

Reusable data analysis skills for DataPaw.

This is a pure asset package. Bundled skill definitions live directly in `skills/` and are loaded by DataPaw host packages from either the source checkout or the installed wheel metadata.
